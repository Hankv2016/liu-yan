# 2026-07-23

- 实现首页搜索文章功能：前端实时过滤，匹配标题/摘要/分类/标签，搜索框 var 输入事件触发，清空恢复全部文章，空结果显示 `no_results` 提示
- 新增 `no_results` i18n key（en: "No matching posts found.", zh: "没有匹配的文章。"）
- 重构首页 JS：将文章数据存入全局 `allPosts`，抽取 `renderPosts()` 和 `buildTagCloud()` 可复用函数
- 后台 `admin.html` 加标签联想（autocomplete）：加载文章时收集去重标签到 `allTags`，`tagsInput` 打字时取最后逗号后片段匹配，弹出下拉建议，支持点击 / ↑↓ 选择 / Enter、Tab 补全 / Esc 关闭
